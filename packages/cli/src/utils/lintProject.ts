// ponytail: thin re-export — lintProject lives in @shiftcut/lint so it's usable without the CLI
export { lintProject, shouldBlockRender } from "@shiftcut/lint";
export type { ProjectLintResult } from "@shiftcut/lint";
